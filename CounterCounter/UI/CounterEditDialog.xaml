<!-- CounterCounter/UI/CounterEditDialog.xaml -->
<Window x:Class="CounterCounter.UI.CounterEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="カウンター編集"
        Width="400"
        Height="250"
        WindowStartupLocation="CenterOwner"
        Background="#1e1e1e">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#2d2d30"/>
            <Setter Property="Foreground" Value="#ffffff"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3e3e42"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2d2d30"/>
            <Setter Property="Foreground" Value="#ffffff"/>
            <Setter Property="BorderBrush" Value="#3f3f46"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Margin" Value="5"/>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#ffffff"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="カウンター名" FontWeight="Bold"/>
        <TextBox Grid.Row="1" x:Name="NameTextBox" FontSize="14"/>

        <TextBlock Grid.Row="2" Text="色" FontWeight="Bold" Margin="0,10,0,0"/>
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="5">
            <Button Content="赤" Click="ColorButton_Click" Tag="#ff0000" Width="60"/>
            <Button Content="緑" Click="ColorButton_Click" Tag="#00ff00" Width="60"/>
            <Button Content="青" Click="ColorButton_Click" Tag="#0080ff" Width="60"/>
            <Button Content="黄" Click="ColorButton_Click" Tag="#ffff00" Width="60"/>
            <Button Content="白" Click="ColorButton_Click" Tag="#ffffff" Width="60"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="OK" Click="Ok_Click" Width="80"/>
            <Button Content="キャンセル" Click="Cancel_Click" Width="80"/>
        </StackPanel>
    </Grid>
</Window>