<Window x:Class="CounterCounter.UI.Dialogs.ModernMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="メッセージ"
        Width="450" Height="250"
        WindowStartupLocation="CenterOwner"
        Background="#1e1e1e"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True">

    <Window.Resources>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#00d4ff" Offset="0"/>
                        <GradientStop Color="#0080ff" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="MinWidth" Value="100"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#33e0ff" Offset="0"/>
                                            <GradientStop Color="#0099ff" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CancelButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#6c757d" Offset="0"/>
                        <GradientStop Color="#495057" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="#1e1e1e" BorderBrush="#00d4ff" BorderThickness="2" CornerRadius="10">
        <Border.Effect>
            <DropShadowEffect Color="#00d4ff" BlurRadius="20" ShadowDepth="0" Opacity="0.5"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#0d0d0d" CornerRadius="8,8,0,0" Padding="20,15">
                <TextBlock x:Name="TitleText" Text="メッセージ" FontSize="18" FontWeight="Bold" 
                           Foreground="#00d4ff"/>
            </Border>

            <Grid Grid.Row="1" Margin="30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="IconText" Grid.Column="0" FontSize="48" Margin="0,0,20,0"
                           VerticalAlignment="Center" HorizontalAlignment="Center"/>

                <TextBlock x:Name="MessageText" Grid.Column="1" TextWrapping="Wrap" 
                           FontSize="14" Foreground="#e0e0e0" VerticalAlignment="Center"/>
            </Grid>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" 
                        Margin="20,0,20,20">
                <Button x:Name="YesButton" Content="はい" Click="YesButton_Click" 
                        Style="{StaticResource ModernButton}" Margin="5" Visibility="Collapsed"/>
                <Button x:Name="NoButton" Content="いいえ" Click="NoButton_Click" 
                        Style="{StaticResource CancelButton}" Margin="5" Visibility="Collapsed"/>
                <Button x:Name="OkButton" Content="OK" Click="OkButton_Click" 
                        Style="{StaticResource ModernButton}" Margin="5" Visibility="Collapsed"/>
                <Button x:Name="CancelButton" Content="キャンセル" Click="CancelButton_Click" 
                        Style="{StaticResource CancelButton}" Margin="5" Visibility="Collapsed"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>