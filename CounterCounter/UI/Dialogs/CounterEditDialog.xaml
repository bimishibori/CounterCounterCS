<Window x:Class="CounterCounter.UI.Dialogs.CounterEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="カウンター編集"
        Width="500" Height="565"
        WindowStartupLocation="CenterOwner"
        Background="#1e1e1e"
        ResizeMode="NoResize">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#2d2d30"/>
            <Setter Property="Foreground" Value="#f0f0f0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3e3e42"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#f0f0f0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,5"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2d2d30"/>
            <Setter Property="Foreground" Value="#f0f0f0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#3e3e42"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,5"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#2d2d30"/>
            <Setter Property="Foreground" Value="#000000"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#3e3e42"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
    </Window.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="カウンター名" FontWeight="Bold"/>
            <TextBox Grid.Row="1" x:Name="NameTextBox" MaxLength="50"/>

            <TextBlock Grid.Row="2" Text="色" FontWeight="Bold" Margin="0,15,0,5"/>
            <StackPanel Grid.Row="3" Orientation="Horizontal">
                <Border x:Name="ColorPreview" Width="60" Height="30" Background="#51cf66" 
                        BorderBrush="#3e3e42" BorderThickness="1" CornerRadius="4" Margin="0,0,10,0"/>
                <Button Content="色を選択..." Click="SelectColor_Click" Width="120"/>
            </StackPanel>

            <Border Grid.Row="4" Background="#2a2a2a" CornerRadius="6" Padding="15" Margin="0,20,0,0">
                <StackPanel>
                    <TextBlock Text="ホットキー設定" FontWeight="Bold" FontSize="16" Margin="0,0,0,15"/>

                    <StackPanel x:Name="IncrementHotkeyPanel">
                        <TextBlock Text="増加キー" FontWeight="Bold" Margin="0,5,0,10"/>
                        <StackPanel x:Name="IncrementHotkeySlotsPanel"/>
                    </StackPanel>

                    <StackPanel x:Name="DecrementHotkeyPanel" Margin="0,20,0,0">
                        <TextBlock Text="減少キー" FontWeight="Bold" Margin="0,5,0,10"/>
                        <StackPanel x:Name="DecrementHotkeySlotsPanel"/>
                    </StackPanel>

                    <TextBlock Text="ヒント: 最大3セットまで登録可能。例: Ctrl+Shift+↑" 
                               FontSize="11" Foreground="#808080" Margin="0,15,0,0" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>

            <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="OK" Click="OkButton_Click" Width="100"/>
                <Button Content="キャンセル" Click="CancelButton_Click" Width="100"/>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Window>