<Window x:Class="CounterCounter.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:CounterCounter.UI.Views"
        Title="Counter Counter - Settings"
        Height="600"
        Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#1a1a1a"
        ResizeMode="CanResize">

    <Window.Resources>
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border Name="Border" 
                                Background="#2a2a2a" 
                                BorderBrush="#3a3a3a" 
                                BorderThickness="1,1,1,0"
                                CornerRadius="8,8,0,0"
                                Margin="2,0"
                                Padding="20,12">
                            <ContentPresenter ContentSource="Header" 
                                            HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#0d7377"/>
                                <Setter TargetName="Border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#0d7377" BlurRadius="15" ShadowDepth="0" Opacity="0.6"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#3a3a3a"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="#e0e0e0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#1a1a1a" Offset="0"/>
                <GradientStop Color="#0f0f0f" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <TabControl Margin="20">
            <TabItem Header="カウンター管理">
                <ContentControl Name="CounterManagementContent"/>
            </TabItem>

            <TabItem Header="ホットキー設定">
                <ContentControl Name="HotkeySettingsContent"/>
            </TabItem>

            <TabItem Header="接続情報">
                <ContentControl Name="ConnectionInfoContent"/>
            </TabItem>
        </TabControl>
    </Grid>
</Window>